# Netflix-Recommendation-System
A content-based movie recommendation system that suggests titles based on genre similarity. Uses TF-IDF to convert genres into numerical vectors and cosine similarity to find the most similar movies. Works case-insensitively and requires no user ratings.
